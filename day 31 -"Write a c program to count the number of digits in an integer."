#include <stdio.h>

int main() {
   int a,c=0;
   printf("Enter an integer: ");
   scanf("%d",&a);
   while(a!=0){
       a=a/10;
       c++;
        
   }
   printf("No. of digits are: %d",c);

    return 0;
}
