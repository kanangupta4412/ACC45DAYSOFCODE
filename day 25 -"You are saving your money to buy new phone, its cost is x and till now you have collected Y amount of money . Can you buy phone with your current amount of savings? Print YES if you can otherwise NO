#include <stdio.h>

int main() {
   int x,y;
   printf("x: ");
   scanf("%d",&x);
   printf("y : ");
   scanf("%d",&y);
   if(y > x){
       printf("Yes");
   }
   else{
       printf("No");
   }
    return 0;
}
