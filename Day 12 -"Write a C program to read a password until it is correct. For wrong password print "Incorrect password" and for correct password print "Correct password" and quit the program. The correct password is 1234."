#include<stdio.h>
void main() {
    int i = 0,pass;
    do{
        printf("Input the password: ");
        scanf("%d",&pass);
        if(pass==1234){
            printf("correct Password");
            break;
        }
        printf("Incorrect password\n");
    }
    while(pass=1234);
}
